import React from 'react';
import Outer from '../../Templates/Outer';
import { Register } from '../../pageListAsync';

function RegisterDedicated() {
  return (
    <Register />
  );
}

export default RegisterDedicated;
